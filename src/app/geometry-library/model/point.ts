export class Point {
  notation: string;
  x: number;
  y: number;
  z: number;

  constructor(notation: string, x: number, y: number, z: number) {
    this.notation = notation;
    this.x = x;
    this.y = y;
    this.z = z;
  }
}
